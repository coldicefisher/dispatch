import { LegalStructurePipe } from './legal-structure.pipe';

describe('LegalStructurePipe', () => {
  it('create an instance', () => {
    const pipe = new LegalStructurePipe();
    expect(pipe).toBeTruthy();
  });
});
